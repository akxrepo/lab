apiVersion: v1
kind: Service
metadata:
  name: httpd-helm-svc
  labels:
    app: httpd-helm-svc
spec:
  ports:
  - name: "80"
    port: {{ .Values.service.port }}
    protocol: TCP
    targetPort: {{ .Values.service.targetPort }}
  selector:
    app: httpd-helm
  type: ClusterIP
