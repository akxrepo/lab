apiVersion: v1
kind: Service
metadata:
  labels:
    app: httpd-kustomize-svc
  name: httpd-kustomize-svc
spec:
  ports:
  - name: "80"
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: httpd-kustomize
  type: ClusterIP
